# September 7 review scope and gap ledger

This is an in-progress follow-up to [the September 6 audit](AUDIT-2026-09-06.md), requested as part of the [active delivery plan](DELIVERY-PLAN.md). Metadata below was refreshed September 7, 2026; it pins review inputs and does **not** mean every listed change has been re-audited. Later heads require their own review and evidence.

## Source scope

Main at the initial renewed review: `0b4559cfab26552b9075f2f14dfd0305a8d872ac` (merged #52). The original September 6 audit covers `f66cd3f`; published v0.1.0 source remains `52fd88d`. Review earlier foundation contracts when recent code depends on them. Closed PRs may have been incorporated by another PR; verify the resulting tree instead of equating closed with merged.

| PR | Head at snapshot | Subject / review focus |
|---|---|---|
| #14 | `fd32591a0f52706f4dd04c238df8c024e99ad6c3` | Durable physical content observations and stale context |
| #15 | `01deee2563da6b60d3d2aa7f8e78c9457352b4f0` | Recovery and durable handoff evidence |
| #16 (closed) | `03f477b78655de42745d894a673ae2b6356d2444` | Journal; trace inclusion in integrated stack |
| #17 | `766f6b1899cf155ecd6e07382e85d9a05dac799a` | Verification, property tests, coverage and performance provenance |
| #18 | `5e6ff424f09a2b6cbff49abf586178702d42a8d6` | Worktrees and scoped authentication |
| #19 | `a40e1eabbfea50e98a092c0af14921e3cec77ad8` | Journal cursors/digests and hook injection |
| #20 | `d7827a1a7803b24d9390f7ceddcf84c7ad9dfe51` | Journal/recovery/container integration |
| #21 | `06852ba234aa67d6c5e2ef84622817013e99798d` | Image builds and durable input provenance |
| #22 | `060b2b16f747063a969db3f1c1d9bea5b1db72fd` | Native socket resolution and client startup |
| #23 | `8c867e2342b228547760fd88e726244248703926` | Addressed handoffs, transfer and cross-host import |
| #24 | `8cccacc8f0b6890f104d151e9f6bb150760d32c9` | Isolation and physical overlap |
| #25 | `f7970b5e52d864f85fa5036272ed50b77391a6f1` | Container lifecycle across daemon crashes |
| #26 | `c03dbf2027f0d3c60faed6a85fcb79363aec96ea` | Short socket paths and optional restricted endpoint |
| #27 | `92474c41cbe33ff9d0593ebd584aaddc5a18c134` | Authenticated workspace, Podman VM, image-aware checks |
| #28 | `3a6dccc93cfb525a56dc99d5e473b5e7d01e4042` | Handoff/socket/engine stack integration |
| #29 | `81011c306d7a10b990796b2fe2afd36b66306792` | Docker Desktop private-volume socket relay |
| #30 | `4df1fb931df5d0b078f77a8e008ce261cc6e3fff` | Documentation structure and product claims |
| #31 | `f4f9a7866c52f3dbca2503c115b0e4e16737e6c4` | Inventory/setup/continuous discovery |
| #32 | `0cdb8baca40980c741f2b1257fa1af2df76c23ac` | Native GUI over OS IPC |
| #33 | `d513943912c69e5badde8442c4d47a7f44f279a7` | Failed scans and verifiable setup |
| #34 | `e5bfb94d08a09bd470ec3c73eda1bf00108af2be` | Channel membership and routing |
| #35 | `45085300c09c275bb316194733d17a88e4a58f2a` | Session plan and MCP response representation |
| #36 | `8c4233907923f352fa007dfb24a49298502051ba` | PTY, native UI, human questions and relaunch |
| #37 | `5ac54c5ce7d03790dd844c26728374850200b5c1` | Fair waiting/deadlock/activity and combined contest/multiplexer changes |
| #38 (closed) | `6039b6f614f9025736f68cb9f3adeac1f3801726` | Contests; verify integration through #37 |
| #39 (closed) | `1adb72d0e11db5389c3d7fd704eeecdab480e8bf` | Multiplexer discovery; verify integration through #37 |
| #41 | `067b0a1128d929d6a9609a3012da1ce1f4089875` | Socket cancellation test determinism |
| #42 | `c9984ad729dbc380144cabc3f1f90f849ec43795` | tmux pane launch and race-claim corrections |
| #43 | `f43ad7b7e702fe6bc846ee4c2447f59b002da964` | agentdocker display name |
| #44 | `54d13d941c3bec055558e937f22897f3c568658e` | Original audit and local trial |
| #46 | `a7531bec77bb673600ec83aa0070a9bc33e12c53` | Restore durability/readiness/private state; merged |
| #52 | `4a211c2e04215120f3484ae76f931bb4422000d3` | Atomic inbox acknowledgement and liveness ordering; merged |

| Open PR | Head at snapshot | Base and outstanding review |
|---|---|---|
| #45 | `dd3da32966ae1ff3011ed2e9a301d4bf1e44c38d` | main; restart/backoff/dependencies and fleet UI; inspect integration in #50 |
| #47 | `6c530eda8550c2c362cdd84e9dc625c437949430` | feat/supervision; policy/quotas; inspect integration in #50 |
| #48 | `d4f0fb5b2b66be7fa7951b202afcb3d29f5f3492` | main; packaging/window; subsequent main integration `8cd6878dc3dd3370d755071a67e25efb2568d472` passed local gate, needs final-head CI/review |
| #49 | `96117c2f4759a2c25079062c83215f071082a530` | feat/native-desktop-delivery; saved preview/apply/undo and daemon diagnostics |
| #50 | `2ad976ba61d1fafcc411facb4ba1beb2e636bd69` | main; supervision/policy/reload/commit/log/UI/multi-checkout stack, actively changing in another worktree; review without overwriting it |
| #51 | `c3884e83a29a829517c418166da0c26a4759936b` | feat/guided-setup; immutable releases, pinned activation, rollback and compiled schema identity |
| #53 | `72f0c726a09cfc3278da2fe049d5e9e16d0201b1` | feat/native-installation; bounded read-only provider wiring diagnostics |
| #54 (draft) | `378d5e0261551dff335946dcef7c29416a46db14` | feat/native-installation; partial Windows foundations, native CI still failing |

Additional owned work: Windows IPC `2d89eeed88051f518979747885c4d56aca7363df` is draft #55. Native launch context `574295f` passed its full local gate and is in review from main. Subsequent Windows owner-token checks at `c903de9` and unique-PID correction at `ca56787` require native CI; the initial snapshot above is retained. Do not label private dirty work as a reviewed commit.

## Initial gap register

| ID | Finding / status | Required closure |
|---|---|---|
| G01 | Restore lease/readiness/private-state defects reproduced and fixed in merged #46; failed cleanup and event-ordering review findings also fixed. | Preserve regressions while integrating sustained-use stack; candidate/platform trial. |
| G02 | Process spawn and SQLite PID persistence are not atomic. Post-spawn failure kills/reaps the owned process, but a child may execute briefly first; crash boundary remains. | Gate user execution or establish an equally strong recoverable ownership protocol; test crashes on both sides and prove no unprotected/duplicate writer. |
| G03 | Destructive inbox operations lost durable event/liveness ordering; reproduced and fixed in merged #52. | Preserve atomic acknowledgement under storage faults; do not equate server acknowledgement with provider consumption. |
| G04 | Supervised native child can inherit the wrong daemon home and stale restricted token/autostart policy. Real initial-launch regression failed with exit 71 before the fix. | Verify initial and restored launch through the actual CLI/daemon with deliberately conflicting private configuration; fix/docs/final gate/review. |
| G05 | Original macOS packaging wrote FinderInfo after signing and invalidated the final bundle. Fixed packaging now verifies extracted final archives; original failed artifacts retained. | Final signed/notarized public candidate with actual Developer ID identity. Local adhoc preview is insufficient. |
| G06 | Guided setup/installer/health exist in open stack, while product/trial docs still described direct GUI setup with no undo. | Update docs to distinguish main, feature stack and release; preserve user edits and test interruption/undo/config health. |
| G07 | Windows foundation native CI at 378d5e0: 132/133 passed, one current-user process snapshot failure. Earlier run had five failures; path/fixture fixes and diagnostics reduced this, but passing cancellation rerun alone does not diagnose its original timeout. | Diagnose process identity and the original cancellation fixture timeout, remove temporary diagnostics when understood, pass strict native tests. Whole daemon/GUI/ConPTY/service/installer still need implementation. |
| G08 | Windows named-pipe IPC has strict Mac regression coverage and cross-compiled native tests, but no native Windows runtime evidence at this snapshot. | Run ownership/ACL/remote rejection, duplex, timeout/shutdown/admission tests on Windows; port consumers and verify full native product. |
| G09 | **Reproduced against #50 source 2ad976b:** actual reload returned success and both owned batch/PTY agents exited before their continuation marker. An isolated diagnostic disabling kill-on-drop let the PTY continue, but its new log output was lost; batch continuation still failed, and the predecessor was alive after 12 seconds. Original source restored after diagnosis. | Correct child ownership, batch-pipe transfer, terminal logging and runtime teardown. Complete successor acknowledgement and activated-binary selection. Diagnostic kill-on-drop disabling is not a production fix. Add actual binary lifecycle regressions. |
| G10 | Separate CLI/desktop identities, Linux application inventory, broader installation paths and capability reporting remain incomplete. | Per-adapter truthfulness tests, native app launched without shell PATH, actual installed/running/registered/consuming distinctions. |
| G11 | Comprehensive local-trial and testing-standard matrix has not been executed. Existing benchmark covers contention/release, and current fuzz targets cover only part of the planned inputs. | Complete T01–T12 and L01–L15 in DELIVERY-PLAN.md; add missing harnesses and candidate/platform evidence. |
| G12 | Installer standard gate had an initial five-second fixture timeout; 30 diagnostic repetitions passed with deadline unchanged. A separate earlier gate was terminated. Causes remain unestablished. | Preserve original failures and diagnostics; investigate recurrence. Neither a rerun nor G04 establishes their cause. |
| G13 | Stock macOS nextest captured a sibling's stdout pipe; endpoint evidence identifies Darwin pipe/CLOEXEC spawn race. Host-macOS test group serialization preserves 500 ms failure and internal product concurrency; intentional leaked child still fails. | Preserve scoped workaround and original evidence while merging; do not apply diagnosis to unrelated failures or relax leak thresholds. |
| G15 | #50 automatic restart completion mutates Running state and emits a separate event without cleaning up the child when persistence fails. Existing supervisor prelaunch validation does check storage; the post-spawn path still needs correction. Restart-policy cancellation also needs rechecking after asynchronous preparation. | Reproduce storage-event faults and stop during preparation, make transitions atomic, retain owned cleanup and prevent duplicate/surviving writers. |
| G16 | #47/#50 policy loading collapses metadata errors into file removal and reads without a size/type bound. Initial invalid-policy behavior and durable policy-change events also need review. | Distinguish confirmed removal from unreadability, test initial and later failures, reject special/oversized files without blocking the daemon, and align events/docs with the contract. |
| G14 | Distribution still lacks public signed current release, maintained Homebrew tap/cask, Linux distribution packages and native Windows download. Update feed, uninstall/retention and seamless activated-binary upgrade remain. | Implement, verify final payloads and publish only supported channels after candidate acceptance. |

## Selected existing evidence and its limits

Private raw logs, provider transcripts, setup receipts, screenshots and machine identifiers remain outside GitHub. This table is a sanitized starting index, not the completed test matrix.

| Source / work | Observed evidence | Limit |
|---|---|---|
| #46 a7531be | 392 Rust / 5 Python, full local gate, final CI and CodeRabbit disposition | macOS local plus CI; unresolved spawn/database atomicity remains |
| #52 4a211c2 | 395 Rust / 5 Python, full local gate, final CI and CodeRabbit disposition | Legacy drain is server acknowledgement, not verified provider consumption |
| Desktop 8cd6878 | 395 Rust / 14 Python, full local gate | Final integrated-head CI/review pending at snapshot |
| Desktop 4f10486 | Clean-source macOS package and actual responsive window; 11 TCP samples across 1.59 s | Bounded graphical check, not soak or public signing |
| Installer e6a19aa | 410 Rust / 17 Python; clean package and distinct-source install/update/rollback with responsive existing fixture daemon; tamper/stale refusal | Explicit local preview, retained old version; not seamless daemon replacement or automatic updates |
| Health 3972761 | 417 Rust / 17 Python; malformed trailing MCP args reproduced/fixed | Bounded config/executable inspection does not execute provider or prove consumption |
| Provider source a61a14c | Claude Code 2.1.263 six-hook consumed-token trial; Codex 0.153.4 five-tool consumed-token/peer/journal trial | Old exact source, short fresh sessions; initial Codex approval failure preserved; full current candidate still required |
| Windows foundations 378d5e0 | Native core/host: 132 passed, 1 failed; ACL/lock/path tests passed | Failing run, no complete Windows product; original cancellation timeout not explained by its passing rerun |
| Windows IPC 2d89eee | 410 Rust / 17 Python macOS full gate; Windows strict cross-Clippy; five Mac server protocol tests | Four Windows IPC runtime tests await execution |

The authoritative commands, workload and provenance requirements are in the testing standard and delivery plan. Expand this ledger with code review results, missing tests and final integrated acceptance as work proceeds.

## Follow-up evidence

The renewed plan's source `3e127bd` passed the full macOS standard gate (410 Rust / 17 Python, separate doctests, formatting, strict Clippy, packaging and release build). All six changed documents' local links resolved. Native launch context `574295f` passed 396 Rust / 5 Python plus the same full gate; its actual initial-and-restored fixture failed before the fix and passed after it.

Windows failure diagnosis: inspecting the current process passed its PID twice to `sysinfo::refresh_processes_specifics(..., remove_dead_processes=true, ...)`. The dependency clears the updated bit while visiting requested PIDs; the second visit removes the live process. This explains the misleading missing-owner error. `ca56787` makes the selection unique and removes temporary dependency debug logging. Native token checks at `c903de9` additionally ensure ownership before command/cwd metadata reads. Await native execution before calling the correction accepted. The earlier PowerShell fixture timeout remains a separate unexplained failure.

## Delivery execution checkpoint: September 7, resumed work

Main advanced to `2eaea0ed151935e93b00b6b709cf34bce0eb9e49` through merged #48/#50/#49 and #56. #58 was closed automatically when its base branch was deleted; its restart fix was not merged. PR #59 preserves that fix and refuses destructive reload. Its original `97951e16f0c1c6c75c2b4b59ccafdc9a05d39ccb` passed the complete local gate (479 Rust/19 Python) and all executable CI checks. The CLI review fix `d46823f0c13bcd1b266fdf4f10aa59e95190df75` also passed the complete local gate and now refuses autostart when the daemon is absent. Subsequent documentation correction `e33b4d8` removes another stale completion claim. Final CI/review remain required.

G09 was reproduced again at integrated `63c6fbf66dbd2668acda2da138f64744f66ad864`: `reload` returned OK, predecessor exited zero, both batch and PTY continuation markers and log lines were absent. The replacement marked both exited. The new real-binary regression verifies that refusal preserves both PIDs, subsequent execution and log output. This mitigates the destructive command; it does not deliver live replacement.

G02 now has a pre-exec gate implementation on `fix/durable-launch-gate`, under verification. The original strengthened initial-launch test failed because event failure left Running in memory/durable state. Gated initial/restore/restart event-fault tests now execute no command, and actual parent SIGKILL before activation aborts the gated child. The first full gate at `f18d3d4` had 483/484 passing; the remaining failure caught lost restore-specific error context. A new cancellation test then reproduced a surviving descendant after its leader exited; cleanup-before-reap and restore context fixes are under a new full gate. Do not treat the intermediate source as an accepted candidate.

A related native-exit gap was reproduced while integrating the gate: failed status persistence removed leases from memory. Exit now stages record, lease deletion, journal entries and channel closure in one transaction. Focused faults at event, lease, channel and journal writes preserve all prior durable/memory state after owned processes are verified gone. Wider crash/reboot recovery timing and full platform acceptance remain open.

Installer integration `fa90a29c562a782bbfc66e5ec631185c8f55b402` passed 489 Rust/22 Python tests, strict Clippy, doctests, packaging and release build. Actual Mac GUI acceptance passed with 12 inventory rows, fixture discovery, screenshot capture and 11 TCP observations over 1.50 seconds. Published `8683c2769062b410ab832cfe434b2e564901713a` incorporates the concurrent merge and has exactly the same Git tree (`3db10ccdc15b643069d246ba7dfe7eb42c1bdc61`). A prior Mac CI window timeout at `9e42ae7` remains unexplained; empty logs and a generic capture failure did not identify its unmet condition.

Windows foundation `85702a5f4a207871559de7e5bbec56dcc80c26f9` passed native CI (133 tests, plus two invoked fixture entrypoints excluded from standalone enumeration). Shared IPC `628c5da162f974af81f94a00a19945ab57e7e1a4` passed native CI (137 tests with the same helper exclusions), including all four named-pipe runtime scenarios. Full Windows daemon/GUI/process/terminal/service/installer parity remains unfinished. These replace the earlier failing foundation/IPC snapshots, not the full platform gate.

Acceptance diagnostics branch `4f1f211` preserves primary workspace launch errors when cleanup inspection finds no agent; the earlier Podman failure had lost its real cause that way. It records bounded fixture logs and exact GUI readiness conditions. The 100-client Linux benchmark failure was an unlabeled EAGAIN after successful 1/10-client workloads; operation-specific errors and bounded daemon output are now added, with full verification and fresh load/fuzz campaigns pending. No passing rerun is used as a root-cause explanation.

## Integrated delivery checkpoint: launch, policy and diagnostics

Main is now `ffd5b591adc6ccb526d6ff0c22f024fcbfa1614c`, including merged installer #51 and reload safeguard #59. #59 final `e33b4d893ebea0378fb3aabffa5b980fb6dd2afd` passed executable CI and CodeRabbit final-head coverage; the remaining reload wording and CLI findings were corrected before integration. Live replacement is still unfinished.

PR #60 at `f4c5c884ba3a5427ee39582958f77d77ec50ffac` integrates the durable native launch/exit fixes with current installation. The full local gate passed **495 Rust/22 Python**, zero retries, 500 ms leak failure, separate doctests, strict Clippy, packaging and release build (457.26 s). All executable CI passed; CodeRabbit was rate-limited and has been requested again. This is not final review approval or full crash/reboot acceptance.

PR #61 at `6f48e0ad5d4e96eb2597d9f710afbc65f42e60fe` passed **489 Rust/24 Python** and the complete standard gate (378.09 s). It preserves original container failures when inspection fails, bounded fixture logs, graphical readiness fields and operation-labeled socket failures. Fuzz lock resolution and before/after provenance are now checked. Both existing 60-second targets passed on clean `3503bd2d0f924d6c1d25868b56290cfcb1c808b2` with matching manifests, nightly rustc `1.100.0-nightly (0ed41eb41 2026-09-04)` and cargo-fuzz 0.13.2. The earlier campaign updated a stale core dependency entry in the fuzz lockfile and is not clean-source evidence. The original graphical timeout and Linux 100-client EAGAIN remain unexplained.

G16 has a reproduced failure: a policy symlink loop returned an empty policy, erasing the previous deny rules. `3c3710a` implements bounded regular-file reads, preservation of last good rules, refusal for initially invalid policy, and durable `policy_updated` events before memory changes. Ten focused tests and strict workspace Clippy passed. A full run changed source during integration from `3c3710a` to `9f37ed5`; its result is retained as **mixed-source evidence only**. A new standard gate with before/after source identity is running at `9f37ed5bfebbf73cf4380a7050d033a1d8778b38`.

G18 is newly reproduced: a native run returned Running despite `deny = ["run:**"]`. Admission enforcement for native/tmux/container launches, restore/restart checks, and first-project policy loading are being implemented on `fix/run-policy-admission`. Focused native/pane/container/first-claim checks pass; restore/restart refusal assertions are still under investigation. Do not claim this branch accepted.

The remaining #53 runtime-attribution finding is valid and reproduced: a Gemini MCP entry naming Codex was classified as a recognized launch. The fix verifies the inspected runtime in inventory, health and setup previews. Eleven host runtime and fourteen setup tests pass. The first full run found two CLI callers of the changed matcher and stopped at compilation; those callers are fixed, and final source `76eef4e` is under a new full gate. No provider commands or real configuration files were changed by these fixtures.


The renewed open-PR review found another contradictory delivery claim in #62 at `ae671527d405e305f50f175f92d09fb88d43fc10`: its product-direction edit calls descriptor handoff delivered even though merged #59 returns `Unavailable` and removes the destructive implementation. The PkgInfo addition is separate from that claim. Its PR body's notarization/icon conclusion is correlation, not a demonstrated causal test; no Developer ID experiment is recorded. Lowercase product naming and consolidation of the legacy uppercase bundle/curl install path with the native installer remain distribution work. The product direction now keeps live replacement explicitly unfinished and distinguishes signed public distribution from source/local-preview installation.

G16's new fixed-source integrated gate at `9f37ed5bfebbf73cf4380a7050d033a1d8778b38` passed **499 Rust/22 Python** plus the entire standard gate (453.64 s), with matching clean source hashes before/after. PR #63 is stacked on #60. G18's initial restore/restart assertions were fixture/expectation errors: the exited restore fixture lacked a saved restore point, and the restart refusal correctly named the rule without the literal word `policy`. Corrected fixtures and exact action assertions now pass all sixteen focused cases plus strict workspace Clippy. Full source `7e15150` is under verification.

The private-umask engine test in #61 reproduced an additional product defect in both Linux CI relay jobs. A real Podman VM image trial at source `6f48e0a` confirmed the cause: the generated relay source was copied into the image as root-owned `0600`, so the workspace UID could not read `/relay.py`. The original recipe exited 2 with permission denied; an explicit `0444` image-source mode produced the readiness frame and exited zero. The host's private umask is retained. Fix `7304c69` is under a new complete standard gate; final Docker and Podman CI remain required. The owned test VM was stopped after the experiment.

T03 review began with #60's CI LCOV artifact (run `34153069851`). Native launch, restart, restore, supervision and access/error branches were inspected; missing runtime coverage remains, especially restricted-server paths and restart failure transitions. T05's #61 Linux CI benchmark (run `34153325215`, clean merge source `72df687962200c6126fb288f6b11890322a32522`, matching manifests) passed 1/10/100 clients. The 100-client p50/p95/p99 claim-and-release latency was 41.01/85.68/109.75 ms, throughput 2164.28 operations/s. This is a four-CPU Azure Linux testbed, advisory contention data, not the unresolved original EAGAIN diagnosis or a complete stale/restart/soak workload.


## Combined-main review checkpoint: September 7, afternoon

Main `3b9a1b7a9c8d7a6174a989902dd22a025115c5b5` includes #60–#65 and
Windows #54/#55. Merge commits were checked directly: #60 included
`f4c5c884ba3a5427ee39582958f77d77ec50ffac`, not the later review fixes.
CodeRabbit's green statuses on #63/#65 meant **review skipped** because their
base branches were excluded. A review request after merging was refused as
closed. These are outstanding review-coverage gaps, not approvals. #64's final
review found an additional valid inventory-state defect.

G02/G15 follow-up `f48af55b14e02037fe500976582b7f2bdd0f96de` passed the
unchanged-source standard gate: **498 Rust/22 Python**, four skipped
manual/helper fixtures, 422.62 seconds. It retries gate interruption within a
fixed deadline, preserves queued pre-exec errors and awaits SIGCHLD instead of
polling each agent 40 times a second. Before-fix tests reproduced signal
cancellation and a gate error hiding the actual child failure; additional exit
before/after subscription tests pass. This commit was pushed after #60 merged;
the current follow-up branch carries it into combined main.

G16 `9f37ed5` passed its clean full gate (**499 Rust/22 Python**, 453.64 s).
G18 `7e151505d9012606939d8da61ab6c8e444131800` passed its clean full gate
(**505 Rust/22 Python**, 516.85 s), plus all executable CI. Runtime attribution
`76eef4ed5809cf9e2089d0312b6d7c9dd95cbbeb` passed its clean full gate
(**497 Rust/22 Python**, 569.05 s). These supersede the earlier pending local
runs, while their review findings and limits stay recorded.

The relay readability fix `7304c6956402eb235266f0c2bc09a0c8648bc6b1` passed
**489 Rust/24 Python**, 420.13 s, and separate Docker/Podman CI including the
engine-volume relay. The owned Podman VM reproduction remained stopped after
cleanup. Review found container logs were still left in the disposable root:
the follow-up retains them beside the CI result with a 2 MiB tail and fixed
30-second deadline, recording exit/timeout/truncation and secondary diagnostic
errors. Six focused tests pass, including noisy output and an owned hung
process. The suggestion to add root volume ownership initialization is declined
for this fix: the socket is 0600, the writer mount is read-only, and only the
confined relay writes the label-verified volume. Its sticky 1777 rendezvous
directory contains no credentials; engine administration is trusted. The
existing boundary and the separate initialization design are documented in the
container test README.

T03 found a credential durability defect: failed grant event persistence left a
credential stored, and corrupt stored credentials were hidden as authentication
failure. #66 `2d67d4f25642d63533dfd65318c5f8bbada25ab7` commits grant/revoke
and events together and reports/freezes storage failure. Both reproduced
regressions now pass, as do separate revocation faults and real restricted
socket authorization. Its clean standard gate passed **498 Rust/22 Python**,
495.80 s; review/current-main integration are pending.

The #64 inventory follow-up distinguishes `unverified` MCP configuration from
missing setup in the protocol, CLI and native window. Before the fix a valid
alias hid a conflicting reserved runtime and inventory returned `yes`. The new
JSON/TOML regression verifies preserved bytes, malformed/disabled entries,
conflicting runtime names and wire serialization. All 24 focused runtime/setup
checks pass after correcting the changed MCP expectation and restoring a
hook-specific assertion inadvertently changed while editing. The two failed
intermediate runs remain in private evidence. Final combined verification is
next.

T05/T10: clean Mac source `7304c69`, matching before/after manifests, completed
1/10/100 clients. For 100 clients, **10,000 attempts included 9,876 conflicts**;
the mixed p50/p95/p99 was 1039.58/1889.29/2259.90 ms and throughput 91.88
attempts/s. The earlier Linux numbers in this ledger likewise mix successful
two-request cycles and one-request conflicts, so their label “claim-and-release
latency” is imprecise. These results do not establish pure successful-cycle or
actual hook latency. A new benchmark separates outcomes and shared/disjoint
paths with explicit counts; the one-second hook contract still needs a direct
load test. macOS and Azure Linux remain different testbeds.


The combined follow-up at `fd3784fb1a129f5e7ec49dc9415e4ac92757da92`
passed the unchanged-source macOS standard gate (**520 Rust/28 Python**, four
manual/helper skips, 477.53 s). Windows cross-Clippy also passed. Actual native
Windows CI (run `34157790314`) then passed **159/160** and reproduced another
G16 defect: equal-size replacement policies with equal timestamps were treated
as unchanged. The correction uses volume/file identity and change metadata from
a native handle, and the strengthened regression explicitly preserves mtime.
The original Windows failure stays retained; native execution of the fix is
required. All other executable PR #67 checks passed at the earlier source.

Socket outcome branch `7ab32a117a059e11ddbe4109637fb14cb290b4dc` passed
its unchanged-source standard gate (**520 Rust/28 Python**, 461.56 s). A focused
real-daemon workload at `06044b9` recorded 20 attempts per scenario: shared paths
had 10 successful cycles and 10 conflicts; disjoint paths had 20 successes and
zero conflicts, with absent conflict percentiles. Source manifests matched.
Full 1/10/100 campaigns, actual hook latency and final parent integration remain.


### Final follow-up review: health shapes and bounded diagnostic cleanup

CodeRabbit reviewed `6361dcc` in #67 and identified health/inventory disagreement for parsed but invalid MCP containers and an unbounded diagnostic subprocess reap. Both were reproduced with focused regression tests: `[]`, `mcpServers: []/null`, and TOML scalar/array containers were inventory `unverified` but health `missing`; a simulated post-kill reap that never completed exposed the unbounded wait. Health now preserves those configurations as `unverified`. Diagnostic cleanup has a 500 ms reap bound and retains captured output even if reaping fails; failure remains secondary to the original scenario error. This simulation verifies handling, not reproduction of a kernel-stalled process.

Review finding #3952464367 proposed removing `poll` because POSIX allegedly does not list it as async-signal-safe. That premise is false: the [POSIX-required list in Linux signal-safety(7)](https://man7.org/linux/man-pages/man7/signal-safety.7.html) explicitly includes `poll`, `read` and `clock_gettime`. The fixed monotonic child deadline and EINTR handling remain. No launch behavior was changed for this suggestion; Linux/macOS signal-interruption and launch-authorization regressions remain required.


### Lease admission, renewal and release atomicity

The load-path audit reproduced five failures in the `7d5714a` lease implementation: rejected `lease_claimed` replay left a newly persisted lease; rejected renewal replay left extended expiry for both repeated claim and explicit renew; rejected release replay retained SQLite protection but had already removed memory protection; rejected agent writes advanced `last_seen` only in memory. Existing release fault tests checked post-restart protection and publication but omitted the live memory invariant.

Lease activity now commits liveness, new/renewed lease and replay event in one SQLite transaction. Conflict activity also commits liveness with its first conflict event. Core planning remains pure and synchronous; memory and publication advance after the commit under the daemon mutex. Failed releases retain memory protection and journal sequence state. Generic liveness updates and standalone event sequences also advance only after persistence. SQLite synchronous FULL remains unchanged; fewer separate commits are not a measured performance claim. Focused failure tests reproduce the original gaps and check lease-row, conflict-event and deletion faults; full integration, load profiles and broader crash acceptance follow.


The later #67/#70/#71 review also required a retained owner after the 500 ms
post-kill wait expires. The diagnostic helper now starts a daemon reaper before
returning the cleanup failure. A controlled wait fault reproduced the missing
owner (one failure in seven tests); the corrected seven-test suite passed and
verifies eventual reaping, the exact 500 ms wait and preserved diagnostic bytes.
The child startup allowance is one second so process creation is not confused
with the production cleanup deadline. Private evidence:
`ad-late-reaper-baseline.log` and `ad-late-reaper-fixed.log`. This is fault
injection, not a claim to have reproduced an uninterruptible kernel wait.

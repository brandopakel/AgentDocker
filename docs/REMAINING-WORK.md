# Remaining engineering and release work

This is the active backlog for the `0.2.0-beta.2` source candidate. Source,
installation and acceptance are separate: use `agentdocker desktop status` and
`agentdocker daemon status` to identify what a machine actually runs. A newer
commit does not turn an older trial into evidence for different binaries.
Detailed trial pins remain in [the verification index](verification/INDEX.md)
and linked PRs; this file keeps current obligations and unresolved failures.

**The rollout is not complete.** The user requires macOS, Linux and native
Windows. A labeled portable Windows preview can collect feedback while its
installer and service are unfinished; it does not fulfill the full Windows
requirement. Stable macOS distribution also requires credentials and independent
hardware acceptance that are not available yet.

## Release and first run

| Requirement | Current evidence | Remaining work |
| --- | --- | --- |
| Integrate reviewed implementation | Main through #235 includes release size optimization, preview-feed publication, browser egress refresh, setup UX, resumable usage discovery and independent Ubuntu evidence. The beta.2 integration brings together the separately reviewed pruning, usage recovery, shutdown retirement, Windows setup/MCP startup and UX changes. Managed Claude session binding has native Windows and actual-provider acceptance on its pinned CI package; the combined candidate still needs validation. | Complete substantive review and final-head local/CI checks for the integrated candidate, including all valid CodeRabbit findings. A rate-limited review status is not a substantive review. |
| Publish the preview | `v0.2.0-beta.1` remains at `d46db1f2`. Its [protected-tag run](https://github.com/brandopakel/AgentDocker/actions/runs/35763185013) failed the combined CLI/daemon size gate; nothing was published. Source now targets matching `0.2.0-beta.2` packages/tag. Separate `channel-preview` publication and repair are implemented. | Publish the verified integrated beta.2 tag, verify actual hosted archives/checksums/provenance, explicit-version installation, update and rollback. Preserve the failed tag. Keep the prerelease off GitHub's stable latest release, the stable feed and Homebrew tap. [Distribution contract](DISTRIBUTION-SETUP.md). |
| First run on each supported platform | macOS has local installed-package evidence. Independent Oracle Ubuntu 24.04.4 x86_64 passed 26 install/configuration checks, 31 graphical checks/579 actions and seven actual systemd service checks on the old `d46db1f2` CI archive. Physical Windows AWBP passed 61 native checks and actual Claude idle/busy/draft receipts on managed-binding package `137547c0`. | Repeat against the actual hosted candidate: fresh configuration, provider authentication/trust/channel consent, idle/busy message delivery, preserved unsent drafts and restart/reopen. Existing-account reuse and synthetic providers do not prove a fresh provider account or OS user. |
| Independent hardware/distributions | Oracle is a minimal Ubuntu VM; its graphical trial used private dependencies and Xvfb. AWBP is Windows 11 Home build 26200 x64. Local Mac, Intel/ARM CI and Rosetta results retain their own pins. | Independent second Mac, physical Intel, normal target Linux desktop prerequisites, physical Linux input and current-candidate service/provider trials. CI and virtual display results do not close physical acceptance. [Trial procedure](LOCAL-TRIAL.md). |
| Stable macOS signing | Developer ID/notarization is not configured; the September 19 identity check found no valid signing identity. Local previews are ad-hoc signed. | Configure private Developer ID/notary credentials; sign, notarize, staple and test the final app/DMG with Gatekeeper on an independent Mac. The explicit unsigned prerelease route remains separate. |
| Useful tester reports | The trial issue template is merged (#198). | Use actual app/daemon/runtime versions and reproducible steps; omit private prompts, project paths and credentials. |

## Provider reliability and parity

| Requirement | Current evidence and what remains |
| --- | --- |
| Idle delivery and existing sessions | Recorded managed Claude consent/idle and bounded Codex idle/active trials pass. Windows Claude 2.1.280 accepted private-workspace/channel consent and passed four correlated replies with automatic durable receipts on `137547c0`, including an unsent draft and a message queued during a real tool call. An actual daemon restart and CLI reconnect preserved identity, conversation and the original queued ID with two more automatic receipts after channel consent. Root-only verified startup binding fixes the missing session ID exposed by the earlier `147b20f7` receipt timeout. Finish current hosted-package acceptance, broader reopen/runtime coverage and other supported adapters. Claude still prompts for development-channel consent on reconnect. Plain hooks/MCP alone do not wake a model. [Claude](CLAUDE-CHANNEL-INPUT.md), [Codex](CODEX-INPUT.md). |
| Identity and uncertain delivery | Resumption must preserve identity, original queued IDs, questions, observations and task ownership; unsafe live/subscribed cases refuse. The September 21 retained-hook stall and child-hook misdelivery have fixes and bounded root/child evidence on `613c28c4`. Manual readback of a stranded message was not a native receipt. Finish sustained backlog, receiver replacement, prolonged tool/approval waits and uncertain-write recovery across actual provider versions. |
| Project pause/resume | Source and recorded Codex fan-out/managed Claude project input pass within their trial scope. Verify every intended recipient reaches a safe boundary, replies in the original chat and resumes during idle, busy and account-limited states. Preserve missed CLI/global and app/project episodes; queued routing alone proves neither pause nor receipt. |
| Throughput and drafts | Conversation/thread, session/channel, question and Board drafts exist. Active hook input is bounded to 6,000 bytes at tool boundaries; one outstanding offer and provider polling limit throughput. Finish other form drafts, oversized input, measured bursts and sustained queues, sleep/wake, reboot and restart/reopen with drafts intact. |
| Account-limit recovery | Catalog/runtime classification tests and bounded actual Claude/Codex/mid-tool 429 recovery pass. Actual account resets, unrelated replacement identities, more versions/adapters and sustained use remain. A historical managed Claude model-choice prompt followed by API retry left a head without a new verified receipt; expose waiting states without blind replay or automatic model changes. Unknown quota scope/reset remains unknown. |
| Permissions and review | Local command review and managed-network presentation are implemented. The actual network trial stopped at the provider allowlist before a callback. Finish stdin review, broader permission prompts, MCP elicitation and secret input; preserve human/peer queue ordering through interruption and uncertain writes. [Input contract](CODEX-INPUT.md). |
| Setup and readiness | Saved preview/apply/undo, guarded ordinary CLI undo, named hook diagnostics, shell opt-in and app reconnect exist. Windows native publication/ownership and `.exe` recognition fixes passed all six actual Claude configuration checks on `d736d483`; user configuration was preserved. Fresh-user/account auth/trust/consent/delivery remains. Configuration, adapter contact, receiver attachment and receipt are separate evidence. [Guided setup](GUIDED-SETUP.md). |

## Platform and operational work

| Requirement | Current evidence and what remains |
| --- | --- |
| Full native Windows product | Daemon/CLI, protected storage, managed pipes/ConPTY, owner crash cleanup, terminal reattachment and `.cmd` launchers are implemented. Physical AWBP passed 57 native checks and 30 fresh-home starts on `9064c5f3`, six actual configuration checks on `d736d483`, then 61 native checks and actual idle/busy/draft receipts on `137547c0`. Finish native Codex input, service startup, installer/update/rollback, physical console attach, an actual provider's shim, broader daemon/CLI suites and hosted-package lifecycle acceptance. Direct-protocol managed launch workdirs also need physical-path normalization parity with the CLI. No Windows coworker installer exists yet. The intermittent extracted executable first-start failures (including run 35675002937 and #236) remain unexplained; later passes and longer diagnostics do not establish a fix. [Windows contract](WINDOWS-PORT.md). |
| Token accounting | Collector/protocol/CLI/MCP/UI and resumable discovery are implemented. The frozen 692 MB corpus matched 4,703 unique Claude counters with one explicit Codex gap; the 711-second fixture covered 20 epochs/40,000 records and restarts. Recovery restarts invalid discovery metadata without fencing accepted accounting; terminal source gaps remain visible without invented pending jobs. Production collection is still off. Finish integrated activation, bounded long-term dedupe/baseline storage, standalone scanning beyond the 16 MiB prefix, emitted-byte overhead and sustained installed acceptance. Missing coverage stays unknown; totals are tokens, not money. [Contract](ARCHITECTURE.md#planned-protocol-and-event-additions). |
| Browser integration | Inventory/helper exclusion, scoped OAuth/MCP messaging through actual Claude/ChatGPT accounts, multi-project consent, egress allowlisting and CIMD parsing exist. `--allow-from openai` implements bounded startup/hourly list refresh with last-valid retention. Finish real-account CIMD acceptance, desktop service install/start, installed-service refresh and additional verified vendor identities. A Chrome-extension-driven ChatGPT trial reached the existing connector and verified identity; ChatGPT safety checks then blocked its inbox read before content returned, so no correlated reply/ACK or new CIMD consent was proved. Hosted models poll browser inboxes; the connector cannot initiate their model turns. [Connector contract](REMOTE-CONNECTOR.md). |
| Sustained storage and many-project operation | Ledger caps, rotating session/controller/daemon logs, temporary-directory cleanup and slower unfocused UI polling exist. Automatic pruning follows changed desktop activations and preserves verified active/rollback/pinned builds; the corrected isolated three-package trial passed 13 checks. Installed services conservatively retain all builds, so selective exact service-reference pruning remains. Measure idle CPU, watcher load and storage growth over multi-day actual-provider/many-project trials through sleep/wake and reboot. Prior 20-minute retention and 7.5-hour fixture runs are limited evidence. |
| Shutdown and retained failures | #240 addresses an owner-retirement race found in macOS CI by repeating safe cleanup within the existing shutdown deadline; integrated gates and final CI remain distinct evidence. The separate intermittent macOS terminal writer-failure hang stays open: #185 and final #236 macOS CI timed out in `writer_failure_closes_the_reader_and_preserves_its_reason` after earlier passes; 500 isolated repeats did not reproduce it. The #236 run passed the other 1,378 tests. Keep the historical Iced capture failure, benchmark socket timeout and Linux ARM transport refusal. Bounded Linux socket-observer fixes and later passing CI do not erase those trials. |
| Watcher limits | Removed-checkout recovery passed repeated regressions and installed recovery on `3c8c2e1` with providers unchanged. Complete overnight/many-project acceptance of one FSEvents stream per checkout. |
| Live daemon replacement | Source exists behind `AGENTDOCKER_EXPERIMENTAL_RELOAD`. Retain the gate until real model-service/distinct-source handovers, Claude/other provider polling, attached drafts, output drain and uncertainty recovery pass sustained acceptance. Coordinator-only restart is insufficient. |
| Legacy duplicate reconciliation | Offline preview/apply/rollback is implemented. Recompute from a fresh backup only after required nonhuman records have ended and the daemon is stopped. Never merge by display name or stop live providers merely to tidy the list. Current session resumption is separate. [Setup](GUIDED-SETUP.md). |
| Notifications | Recorded installed foreground/background, hidden-app and cold-launch clicks pass for their exact earlier revisions. Finish physical clicks, bounded history page-back and broader historical/project routes on the final package. [Distribution](DISTRIBUTION-SETUP.md). |
| Accessibility, keyboard and IME | Unlocked-Mac external AX traversal/navigation and synthetic Return submission passed on recorded builds; the earlier locked-screen AX failure also affected a plain AppKit control. Multiline/preedit/Unicode/clipboard widget regressions exist. Finish human VoiceOver/supported screen readers, physical keyboard activation, IME composition, zoom/focus and broader terminal clipboard tests on the actual candidate. Synthetic input and AX actions do not establish human acceptance. [UI contracts](ICED-DESIGN.md). |

Federation/host namespaces, cross-host leases/routing, the herdr focus bridge,
additional adapters, container log following and proposed CLI conveniences remain
deferred proposals. See [architecture](ARCHITECTURE.md#planned-protocol-and-event-additions)
and [containers](CONTAINER-ENGINES.md). The operational sequence remains in
[local trial](LOCAL-TRIAL.md).

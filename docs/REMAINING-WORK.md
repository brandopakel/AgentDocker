# Remaining engineering and release work

This is the active backlog after the `0.2.0-beta.3` preview publication. Source,
installation and acceptance are separate: use `agentdocker desktop status` and
`agentdocker daemon status` to identify what a machine actually runs. A newer
commit does not turn an older trial into evidence for different binaries.
Detailed trial pins remain in [the verification index](verification/INDEX.md)
and linked PRs; this file keeps current obligations and unresolved failures.

The next source candidate is `0.2.0-beta.4`, combining required local Codex MCP
startup, historical Claude usage formats, ARM hashing and terminal-input review.
Its final review, release workflow and hosted acceptance are still pending;
published beta.3 evidence remains pinned to its original binaries.

**The rollout is not complete.** The user requires macOS, Linux and native
Windows. A labeled portable Windows preview can collect feedback while its
installer and service are unfinished; it does not fulfill the full Windows
requirement. Stable macOS distribution also requires credentials and independent
hardware acceptance that are not available yet.

## Release and first run

| Requirement | Current evidence | Remaining work |
| --- | --- | --- |
| Integrate reviewed implementation | Beta.3 is published from `5b146e20`, integrating #266 and #267. The final source tree passed 1,425 Rust tests, 155 Python checks, full native CI and substantive independent review. It includes Windows service/ownership changes, browser service controls, bounded accounting, provider fixtures, sender/session resolution, startup diagnostics and atomic initial-schema creation. | Native Windows Codex #256 remains a separate draft and is not included. Passing startup samples establish reduced schema overhead, not a demonstrated fix for the historical first-start failures. |
| Publish the preview | Immutable `v0.2.0-beta.3` published from `5b146e20`; release run 35945751437 passed every job, including fixed preview-feed promotion. All 28 public assets passed digest/source checks; the fixed feed equals the versioned feed. Explicit-version CLI installation and 13 actual beta.2 upgrade/default-feed/rollback stages passed on macOS ARM64 and independent Oracle Ubuntu x86_64. Stable latest/assets remain v0.1.0 and the Homebrew tap is unchanged. This Mac has the exact published package selected for future launches; its existing `6ba7bf71` daemon and connector remain running, with the experimental reload gate preserved. | Existing Mac beta.2 needs one explicit `--local-preview` upgrade because its staging bug remains in that immutable release; beta.3 default-feed staging passed without the extra flag. Final-package physical/fresh-account and stable acceptance below remain open. Earlier failed tags/runs are retained in the verification index. [Distribution contract](DISTRIBUTION-SETUP.md). |
| First run on each supported platform | Actual hosted beta.3 `5b146e20` passed explicit CLI installation, upgrade/rollback, eight terminal checks, desktop capture, 31 graphical workflow checks and six daily-update checks on this Mac ARM64 and independent Oracle Ubuntu 24.04.4 x86_64. Mac installation/pruning passed 13 scenarios after isolating the fixture account home; the original host-service assertion failure is retained. Native Windows release CI accepted the exact portable archive with 61 checks and desktop capture. Physical AWBP actual Claude idle/busy/draft receipts remain pinned to earlier `137547c0`. | Hosted beta.3 also passed an independent Ubuntu actual-model Codex 0.155.1 managed-input trial: three nonce echoes/exact receipts, active-turn peer steering, and same-thread recovery after an owned controller crash. Existing test-account authentication was reused. Complete current-candidate physical Windows and independent Mac trials, fresh configuration and provider authentication/trust/channel consent, actual-provider idle/busy delivery, drafts and restart/reopen. Existing-account reuse, synthetic providers, CI and Oracle Xvfb do not prove a fresh account/OS user or physical input. |
| Independent hardware/distributions | Oracle is a minimal Ubuntu VM; its graphical trial used private dependencies and Xvfb. AWBP is Windows 11 Home build 26200 x64. Local Mac, Intel/ARM CI and Rosetta results retain their own pins. | Independent second Mac, physical Intel, normal target Linux desktop prerequisites, physical Linux input and current-candidate service/provider trials. CI and virtual display results do not close physical acceptance. [Trial procedure](LOCAL-TRIAL.md). |
| Stable macOS signing | Developer ID/notarization is not configured; the September 19 identity check found no valid signing identity. Local previews are ad-hoc signed. | Configure private Developer ID/notary credentials; sign, notarize, staple and test the final app/DMG with Gatekeeper on an independent Mac. The explicit unsigned prerelease route remains separate. |
| Useful tester reports | The trial issue template is merged (#198). | Use actual app/daemon/runtime versions and reproducible steps; omit private prompts, project paths and credentials. |

## Provider reliability and parity

| Requirement | Current evidence and what remains |
| --- | --- |
| Idle delivery and existing sessions | Recorded managed Claude consent/idle and bounded Codex idle/active trials pass. Windows Claude 2.1.280 accepted private-workspace/channel consent and passed four correlated replies with automatic durable receipts on `137547c0`, including an unsent draft and a message queued during a real tool call. An actual daemon restart and CLI reconnect preserved identity, conversation and the original queued ID with two more automatic receipts after channel consent. Root-only verified startup binding fixes the missing session ID exposed by the earlier `147b20f7` receipt timeout. Finish current hosted-package acceptance, broader reopen/runtime coverage and other supported adapters. Claude still prompts for development-channel consent on reconnect. Plain hooks/MCP alone do not wake a model. [Claude](CLAUDE-CHANNEL-INPUT.md), [Codex](CODEX-INPUT.md). |
| Identity and uncertain delivery | Resumption must preserve identity, original queued IDs, questions, observations and task ownership; unsafe live/subscribed cases refuse. The September 21 retained-hook stall and child-hook misdelivery have fixes and bounded root/child evidence on `613c28c4`. Manual readback of a stranded message was not a native receipt. Independent Ubuntu/native Codex 0.155.1 loopback trials passed lost-output readback recovery and refused stale confirmations without replay. Zero-prompt reopen still fails, including a provider-only trial with AgentDocker absent. A separate `b334cc70` Oracle/Codex 0.155.1 trial passed a 90-second MCP human-answer wait with peer/human steering: the exact answer receipt was acknowledged without a duplicate user turn, with the original thread and queued receipts preserved. PR #271 merged as `ce237075` after the local gate, final native CI and substantive review. Finish sustained backlog, receiver replacement, longer tool/approval waits and uncertain-write recovery across actual provider versions. |
| Project pause/resume | Source and recorded Codex fan-out/managed Claude project input pass within their trial scope. A hosted beta.3 actual-model Ubuntu trial failed to return both pause replies to its private project: local MCP identity initialization rejected the app-server, and the models fell back to an account cloud connector. Both owned sessions were stopped and the misplaced test acknowledgements are recorded. Source now distinguishes inventory filtering from managed MCP ancestry and requires the enabled local MCP entry at startup; the corrected `b334cc70` candidate then passed a 49-second Oracle actual Codex 0.155.1 trial with two recipients (one busy in a real tool call, one idle), four lifecycle receipts and four replies in the original project conversation, held/lifted leases, preserved threads and exclusion of the unrelated project. Cloud apps were disabled only for the test launch and saved configuration stayed unchanged. Actual Mac/Linux no-model startup probes verified the bound local MCP contact, and a Mac startup-timeout control refused input before a model prompt. Verify every intended recipient reaches a safe boundary, replies in the original chat and resumes during idle, busy and account-limited states. Preserve missed CLI/global and app/project episodes; queued routing alone proves neither pause nor receipt. |
| Throughput and drafts | Conversation/thread, session/channel, question and Board drafts exist. Active hook input is bounded to 6,000 bytes at tool boundaries; one outstanding offer and provider polling limit throughput. A separate Oracle `b334cc70` actual Codex 0.155.1 trial drained 32 alternating peer/human messages across a 30-second MCP question: all original receipts remained ordered, every nonce was echoed, the original thread survived and no answer became an extra user turn. It passed in 146.88 seconds with no burst messages left queued. Receipt observation delays were 31.30–111.51 seconds including the held question; this is bounded throughput evidence, not low-latency delivery. The first trial exceeded its sixty-second post-answer drain bound after 23 completed messages and is retained as failed. Two normal managed macOS PTY trials on `120811ed` exposed a canonical-buffer overflow: a 20,028-byte paste produced terminal bells without reaching the bridge's size refusal, and the next valid line remained stranded. The earlier accepted long-line trial explicitly used a raw PTY. Source now uses a bounded Unix terminal editor with mode restoration; native correction acceptance is pending. Finish other form drafts, oversized input across providers, larger sustained queues, sleep/wake, reboot and restart/reopen with drafts intact. |
| Account-limit recovery | Catalog/runtime classification tests and bounded actual Claude/Codex/mid-tool 429 recovery pass. Actual account resets, unrelated replacement identities, more versions/adapters and sustained use remain. A historical managed Claude model-choice prompt followed by API retry left a head without a new verified receipt; expose waiting states without blind replay or automatic model changes. Unknown quota scope/reset remains unknown. |
| Permissions and review | Local command review and managed-network presentation are implemented. The actual network trial stopped at the provider allowlist before a callback. Local terminal-input (`writeStdin`) review shows the terminal, complete escaped input and retained access, with exact human decisions and a versioned no-replay ledger. Clean `79e1a0da` passed 87 focused input tests and the 1,430-Rust/155-Python full gate. Independent Ubuntu actual Codex 0.155.1 trials passed Allow and Deny: exact input arrived only after Allow, Deny wrote nothing, queued peer/human input waited through review and resumed in the original thread, and the answer was not duplicated as ordinary input. Native Mac controls routed the actual question payloads once and retained an unrelated draft through navigation/reopen. The upstream experimental feature was enabled only for these private launches; saved configuration stayed unchanged. The first driver's strict command-wrapper assertion failed before approval and remains recorded. A separate four-minute actual-provider wait on `79e1a0da` resolved the exact Allow response and fixture write, and retained all three original message receipts, but failed the required model-echo assertions: only the peer nonce appeared and the model reported approval still pending. No consumed input was retried; this is not a complete long-wait recovery pass. Final integration review/CI, physical acceptance, broader permission prompts, MCP elicitation and secret input remain; preserve human/peer queue ordering through interruption and uncertain writes. [Input contract](CODEX-INPUT.md). |
| Unprompted coordination across providers | On September 22, five tools got a neutral edit task on a file a peer held, with no mention of AgentDocker. Where AgentDocker sits at the tool boundary, the model coordinated on its own: Claude Code (hook), Codex with the #242 hook adapter, and OpenCode with its plugin (`claude/opencode-runtime`). With the MCP server alone, OpenCode, Goose and Copilot CLI edited the held file: instructions do not stop an edit. Open: a Copilot CLI adapter, Gemini CLI, steering models to MCP `send_message` over the CLI, and a repeat on the hosted beta.2 package. Goose has no hook point. [Verification](verification/INDEX.md). |
| Setup and readiness | Saved preview/apply/undo, guarded ordinary CLI undo, named hook diagnostics, shell opt-in and app reconnect exist. Windows native publication/ownership and `.exe` recognition fixes passed all six actual Claude configuration checks on `d736d483`; user configuration was preserved. Fresh-user/account auth/trust/consent/delivery remains. Configuration, adapter contact, receiver attachment and receipt are separate evidence. [Guided setup](GUIDED-SETUP.md). |

## Platform and operational work

| Requirement | Current evidence and what remains |
| --- | --- |
| Full native Windows product | Daemon/CLI, protected storage, managed pipes/ConPTY, owner crash cleanup, terminal reattachment and `.cmd` launchers are implemented. Physical AWBP passed 57 native checks and 30 fresh-home starts on `9064c5f3`, six actual configuration checks on `d736d483`, then 61 native checks and actual idle/busy/draft receipts on `137547c0`. Hosted beta.2 (`f37998dd`) repeated 61 native checks but lost an automatic Claude receipt because its transcript belonged to Administrators. Every managed Windows launch now sets the suspended child’s default object owner to its own user SID before resume; descendants inherit it, and private-history ownership checks stay strict. The corrected CI package (tree identical to `9d602feb`) passed 61 native checks and actual Claude 2.1.280 idle/draft/busy/queued delivery with four correlated replies and automatic receipts in 111.5 seconds, without external token correction. The earlier allocation failure remains unexplained. Finish native Codex input, service acceptance, installer/update/rollback, physical console attach, an actual provider's shim, broader daemon/CLI suites and hosted-package lifecycle acceptance. Direct-protocol managed launches now resolve supplied workdirs to the same physical identity as registration before launch; daemon regressions cover alias binding and refusal of missing, file and cyclic paths. Hosted-package acceptance remains. A Task Scheduler startup probe exposed cross-session client refusal: the desktop daemon could not query its own user’s SSH process. Pipe-token client identification passed native tests and an isolated desktop-session daemon/SSH-client start, stop and restart trial on `068e9756`-equivalent CI binaries. Per-user Task Scheduler install/start/stop/restart/uninstall and a bounded explicit crash supervisor are now implemented, with ownership/interrupted-update regressions. Task arguments escape typographic PowerShell quotes, and service readiness uses the Windows ten-second deadline. Task Scheduler restart settings alone did not recover failures on AWBP; the explicit-supervisor prototype did. Product lifecycle, login/reboot and managed-provider survival remain distinct acceptance gates. No Windows coworker installer exists yet. The intermittent extracted executable first-start failures (including runs 35675002937, 35821898700 and 35821927798, and #236) remain unexplained. Opt-in startup stages now distinguish home security, SQLite file/connection/WAL/schema/migration/index work and state restoration; bounded logs from every smoke-test home and separately retained failed packages preserve diagnostic evidence. Run 35932937310 reached the coordinator lock at 5 ms but did not reach store-open/listener-ready within 10 seconds for the extracted package's OWNER RIGHTS home; ordinary fresh-home startup and desktop capture passed. Run 35935871942 repeated the failure on an ordinary fresh home: coordinator lock at 4 ms, no store/listener readiness within 10 seconds. Both exact failed packages are retained; the region still includes home security, SQLite opening/schema and state restoration. Manual Windows workflow dispatch now supports bounded fresh-home samples for both ancestry types, stopping at the first failure. Run 35938682959 on `0184e1d8` passed 40 additional fresh homes and all 142 checks, with a 4.203-second slowest start. Initial schema creation now commits once with unchanged FULL durability. A separate native run on `89400171` passed another 40 samples and 142 checks, with median/max startup 188/219 ms and median/max schema phase 39/62 ms. Different CI hosts limit the comparison; historical failure diagnosis remains open. Finer diagnostics and later passes do not establish a fix. [Windows contract](WINDOWS-PORT.md). |
| Token accounting | Collector/protocol/CLI/MCP/UI and resumable discovery are implemented. The frozen 692 MB corpus matched 4,703 unique Claude counters with one explicit Codex gap; the 711-second fixture covered 20 epochs/40,000 records and restarts. Recovery restarts invalid discovery metadata without fencing accepted accounting; terminal source gaps remain visible without invented pending jobs. Installed `f37998dd` collection was enabled and completed its first generation in 51.7 seconds, with partial coverage and no pending files. Accounting-only observed fixtures now cover Claude 2.1.277/278/280 and Codex 0.155.1; cursor upgrades verify the saved prefix before replaying skipped records without recounting accepted samples or inventing a source-change gap. A read-only inventory found 12,074 usage-bearing records from nine older, previously unsupported Claude patches. Sanitized accounting-only fixtures now cover 2.1.246/247/248/251/259/260/261/263/267; cursor v5 verifies the full saved v3/v4 prefix before replay, preserving accepted source identities and totals. Unobserved versions remain unsupported. A private actual-corpus upgrade/restart/rollback/re-upgrade on `0f1be022` preserved all 31,024 baseline rows and every later accepted row, recovered 5,856 additional samples and ended with all 80 v5 cursors. Upgrade and rollback had temporary partial coverage while sources changed; the earlier strict all-cursors trial timed out and remains recorded. Existing historical gaps are not silently removed; production activation and historical-gap reconciliation remain distinct acceptance gates. The accepted CI binary passed an independent Ubuntu capacity trial: a record after 20 MiB, 1,000 rejected records at a lowered private limit, visible coverage gap, working coordination, restart and exact-once replay recovery. A second actual-binary trial reached the unchanged 256 MiB limit with 500,000 synthetic records and preserved accepted totals after restart plus 1,000 more records; peak daemon RSS was 17.8 MiB. Index/page overhead is additional. A separate 75,000-record actual-binary trial preserved all 81 fixture messages but exposed coordination RPCs up to 1,964 ms, beyond the hook budget. The corrected 128-record batches passed the repeated workload: 1,816 messages and 7,264 RPCs, p95 40.90 ms and maximum 222.94 ms, with all 75,000 records counted. At capacity, repeated refused reconciliation retains the coverage gap without appending empty accounting events. The installed current-format upgrade on `a46cd934` completed its first generation in 98.759 seconds, preserved all 25,443 prior retained fingerprints and upgraded all 79 cursors, with 5,061 additional accepted samples; historical/unsupported gaps remain visible. The installed `6ba7bf71` upgrade preserved all 30,717 prior accounting rows unchanged, accepted 16 additional samples and activated the 256 MiB tracking cap with 15 triggers; the read-only snapshot measured 29,776,879 tracked bytes. Finish historical-gap reconciliation, multi-day resource trials, standalone scanning beyond the 16 MiB prefix, emitted-byte overhead and sustained installed acceptance. Missing coverage stays unknown; totals are tokens, not money. [Contract](ARCHITECTURE.md#planned-protocol-and-event-additions). |
| Browser integration | Inventory/helper exclusion, scoped OAuth/MCP messaging through actual Claude/ChatGPT accounts, multi-project consent, egress allowlisting and CIMD parsing exist. `--allow-from openai` implements bounded startup/hourly list refresh with last-valid retention. Desktop browser runtime cards now offer macOS/Linux service enable/start with explicit tunnel choice and preservation of differing existing settings. New definitions are fully staged and synced before atomic publication; concurrent setup cannot expose partial content. The selected tunnel owns the progress label. On independent Ubuntu, hosted beta.2 passed a real one-hour OpenAI list refresh without restart, fresh-list loading after restart and cleanup. The accepted CI package passed service enable/idempotence/recovery and an actual desktop-button Cloudflare quick-tunnel launch with public metadata and non-vendor refusal; only browser inventory was a fixture. Finish real-account CIMD acceptance, final hosted-package/Mac service acceptance and additional verified vendor identities. An earlier Chrome-driven ChatGPT trial blocked inbox access. After normal reconnection and the installed `a46cd934` daemon upgrade, a read-only identity call through the existing connector passed; no new permissions were granted. The blocked write path was not retried, and correlated reply/ACK and new CIMD consent remain unproved. Hosted models poll browser inboxes; the connector cannot initiate their model turns. [Connector contract](REMOTE-CONNECTOR.md). |
| Sustained storage and many-project operation | Ledger caps, rotating session/controller/daemon logs, temporary-directory cleanup and slower unfocused UI polling exist. Automatic pruning follows changed desktop activations and preserves verified active/rollback/pinned builds; the corrected isolated three-package trial passed 13 checks. Installed daemon and connector services conservatively retain all builds, including stopped services with no lifetime pin; selective exact service-reference pruning remains. Hosted Mac pruning initially failed its fixture expectation because the real account connector service correctly protected all versions; the corrected fixture uses a private account home and passed all 13 install/prune/uninstall scenarios with actual beta.2/beta.3 packages. The original failure is retained; no host service was removed. The hosted Linux three-package trial exposed deletion of a stopped connector’s referenced executable; the corrected accepted CI package passed three actual activations with the original executable and unit preserved, then restarted that service successfully. Installed `6ba7bf71` observed for 30 minutes during normal activity averaged 11.66% of one core, peaked at 55.1 MiB RSS and added 45,253 tracking bytes with no database-file growth. A separate three-minute empty private daemon averaged 0.13%. In a same-daemon synthetic control, enabling accounting over 256 MiB of unchanged logs raised steady CPU from 0.14% to 2.99%; this identifies repeated scanning as a cost, not full attribution of the production workload. An ARM-only dependency feature now selects sha2 0.10's runtime-detected hardware SHA-256 implementation, retaining its software fallback; other targets keep their current backend. A six-run alternating 256 MiB record-chain microbenchmark produced identical digests with median 1.071 s software versus 0.112 s accelerated (9.52×). The 29 accounting reader/discovery regressions pass, including prefix changes and large-file resumption. Sequential private-daemon trials over the same 256 MiB/eight-event corpus reduced steady CPU from 3.06% to 0.77% of one core (74.8% lower); both retained exactly eight accounting rows over repeated scans, with about 15.5 MiB peak RSS. The clean ARM candidate also completed a 30-minute private observation over the enabled real log roots: 2.37% of one core, 55.9 MiB peak RSS and sampled ping below 0.4 ms, with concurrent development/build load disclosed. Initial collection took 241.5 seconds and peaked at 187.9 MiB. Tracking/database growth and existing gaps remain explicit in its report. Cross-platform CI passed; sustained installed acceptance remains pending and the running production daemon is unchanged. Preserve prefix integrity, coverage gaps and exact-once accounting while measuring that cost. Measure idle CPU, watcher load and storage growth over multi-day actual-provider/many-project trials through sleep/wake and reboot. A hosted beta.2 Ubuntu fixture observed 1/10/100 projects for 30 seconds each: at 100 projects the daemon used 2.23% of one core and completed children 8.73%, with 1,100 watches and no state-file growth; this is a short baseline, not sustained acceptance. Prior 20-minute retention and 7.5-hour fixture runs are limited evidence. The planned 24-hour hosted beta.3 actual Codex trial stopped after 90 minutes: three echoes completed, then the fourth input was accepted but its provider turn failed with `usage_limit_exceeded`. Preserve the original failed assertion and exact-thread transcript diagnosis; no account-limit recovery or 24-hour pass was established. Its 180 samples showed 110 watches, 14.77 MiB peak daemon RSS and about 0.56% of one core under concurrent short tests. One source-generation accounting gap remains explicit. Later trials must retain provider status and logs before cleanup and distinguish an input receipt from a successful model turn. A separate 90-second candidate preflight passed three actual Codex sessions in three projects with exact local MCP bindings and receipts; its revised 24-hour/24-message trial began September 25 at 04:18 UTC on reviewed `b334cc70` CI binaries and remains pending. It retains provider status, attempted IDs and logs before cleanup and disables cloud apps only for the test launches. |
| Shutdown and retained failures | #240 addresses an owner-retirement race found in macOS CI by repeating safe cleanup within the existing shutdown deadline; integrated gates and final CI remain distinct evidence. The separate intermittent macOS terminal writer-failure hang stays open: #185 and final #236 macOS CI timed out in `writer_failure_closes_the_reader_and_preserves_its_reason` after earlier passes; 500 isolated repeats did not reproduce it. The #236 run passed the other 1,378 tests. Linux CI run 35899082704 also failed the controller-upgrade injected-storage assertion without recording the returned response. The regression now prints that response and observed process identities on failure; this is diagnostic coverage, not a demonstrated fix. Keep the historical Iced capture failure, benchmark socket timeout and Linux ARM transport refusal. Bounded Linux socket-observer fixes and later passing CI do not erase those trials. |
| Watcher limits | Removed-checkout recovery passed repeated regressions and installed recovery on `3c8c2e1` with providers unchanged. Complete overnight/many-project acceptance of one FSEvents stream per checkout. |
| Live daemon replacement | Source exists behind `AGENTDOCKER_EXPERIMENTAL_RELOAD`. An independent Ubuntu/native Codex 0.155.1 loopback trial passed four same-binary handovers while idle, with a draft, with busy queued input and with a pending human question; identity, binding, ordered receipts and exactly-once answer survived. An additional independent Ubuntu trial with the actual Codex model service passed three distinct-source hosted beta.2 ↔ beta.3 handovers: idle upgrade, rollback with active input and idle re-upgrade. The managed controller, conversation and three exact receipts survived; predecessor retirement took 0.194–0.304 seconds and cleanup was graceful. Retain the gate for sustained real-service handovers, Claude/other providers, attached drafts, pending human questions, output drain and uncertainty recovery. Coordinator-only restart is insufficient. |
| Legacy duplicate reconciliation | Offline preview/apply/rollback is implemented. Recompute from a fresh backup only after required nonhuman records have ended and the daemon is stopped. Never merge by display name or stop live providers merely to tidy the list. Current session resumption is separate. [Setup](GUIDED-SETUP.md). |
| Notifications | Recorded installed foreground/background, hidden-app and cold-launch clicks pass for their exact earlier revisions. Finish physical clicks, bounded history page-back and broader historical/project routes on the final package. [Distribution](DISTRIBUTION-SETUP.md). |
| Accessibility, keyboard and IME | Unlocked-Mac external AX traversal/navigation and synthetic Return submission passed on recorded builds; the earlier locked-screen AX failure also affected a plain AppKit control. Multiline/preedit/Unicode/clipboard widget regressions exist. Finish human VoiceOver/supported screen readers, physical keyboard activation, IME composition, zoom/focus and broader terminal clipboard tests on the actual candidate. Synthetic input and AX actions do not establish human acceptance. [UI contracts](ICED-DESIGN.md). |

Federation/host namespaces, cross-host leases/routing, the herdr focus bridge,
additional adapters, container log following and proposed CLI conveniences remain
deferred proposals. See [architecture](ARCHITECTURE.md#planned-protocol-and-event-additions)
and [containers](CONTAINER-ENGINES.md). The operational sequence remains in
[local trial](LOCAL-TRIAL.md).
